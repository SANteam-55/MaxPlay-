--- src/screens/main/MeScreen.tsx
+++ src/screens/main/MeScreen.tsx
@@ -1,7 +1,7 @@
 import React, { useState, useEffect } from 'react';
 import { useAuth } from '../../hooks/useAuth';
-import { subscribeToAnnouncements } from '../../services/contentService';
+import { subscribeToAnnouncements, getUserComments } from '../../services/contentService';
 import { LogoPlaceholder } from '../../components/common/LogoPlaceholder';
 import {
   Crown,
@@ -35,6 +35,11 @@
   const [announcementCount, setAnnouncementCount] = useState(0);
   const [showAdminBanners, setShowAdminBanners] = useState(false);
 
+  const [showCommentsModal, setShowCommentsModal] = useState(false);
+  const [userComments, setUserComments] = useState<any[]>([]);
+  const [loadingComments, setLoadingComments] = useState(false);
+
   // Add effect to simulate fetching unread announcements
   useEffect(() => {
     const unsub = subscribeToAnnouncements((list) => {
@@ -53,6 +58,16 @@
     }
   }, []);
 
+  const loadUserComments = async () => {
+    if (user?.uid) {
+      setLoadingComments(true);
+      setShowCommentsModal(true);
+      const comments = await getUserComments(user.uid);
+      setUserComments(comments);
+      setLoadingComments(false);
+    }
+  };
+
   const handleLogout = async () => {
     try {
       await logout();
@@ -206,12 +221,7 @@
             iconBgColor="bg-[#F59E0B]/15"
             title="My Comments"
             subtitle="Comment history & replies"
-            onPress={() =>
-              setModalInfo({
-                title: 'Comment History',
-                body: 'Track your posted comments and replies across all movies and episodes.',
-              })
-            }
+            onPress={loadUserComments}
           />
         </div>
       </div>
@@ -352,6 +362,39 @@
         />
       )}
 
+      {/* My Comments Modal */}
+      {showCommentsModal && (
+        <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm">
+          <div className="w-full max-w-sm rounded-2xl bg-[#1C1C1E] p-6 shadow-2xl border border-white/10 flex flex-col max-h-[80vh]">
+            <div className="flex justify-between items-center mb-4">
+              <h3 className="text-lg font-bold text-white">My Comments</h3>
+              <button onClick={() => setShowCommentsModal(false)} className="text-[#A1A1AA] hover:text-white transition">
+                <X className="h-5 w-5" />
+              </button>
+            </div>
+            
+            <div className="overflow-y-auto pr-2 scrollbar-none flex-1 space-y-4">
+              {loadingComments ? (
+                <p className="text-sm text-[#A1A1AA] text-center py-6">Loading your comments...</p>
+              ) : userComments.length === 0 ? (
+                <p className="text-sm text-[#A1A1AA] text-center py-6">You haven't posted any comments yet.</p>
+              ) : (
+                userComments.map((comment) => (
+                  <div key={comment.id} className="bg-[#27272A] p-3 rounded-xl border border-white/5">
+                    <p className="text-xs text-[#8B5CF6] mb-1 font-semibold">On Content ID: {comment.contentId}</p>
+                    <p className="text-sm text-white">{comment.text}</p>
+                    <div className="flex items-center gap-4 mt-2">
+                      <span className="text-[10px] text-[#A1A1AA]">{new Date(comment.createdAt).toLocaleDateString()}</span>
+                      {comment.likes > 0 && <span className="text-[10px] text-pink-500 font-bold">{comment.likes} Likes</span>}
+                    </div>
+                  </div>
+                ))
+              )}
+            </div>
+          </div>
+        </div>
+      )}
+
       {/* Generic Info Modal */}
       {modalInfo && (
         <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm animate-in fade-in zoom-in duration-200">
